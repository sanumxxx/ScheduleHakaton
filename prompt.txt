Что нужно далее, сделать заполнение расписанием более удобным, мне нужно чтобы я просто из выпадающего меню выбрал кафедру и группу, а далее уже было кучу инпутов куда я мог вводить пары, через выпадащее меню или автоподставление. Преподаватели должны нормалньо видеть расписание. Преподаватели должны быть добавлены через регистрацию, а в панели только изменены или удалены. Сообщения от преподавателя должны отправляться конкретным группам, думаю нужно будет сделать форму с галочкой, ты выбираешь группу/ы которым писать и оно отправится только им. Индикатор кол-ва заявок я добавил, нужно еще индикатор сообщений.

 with app.app_context():
        db.create_all()
        if not User.query.filter_by(username='admin').first():
            hashed_password = bcrypt.generate_password_hash('admin').decode('utf-8')
            admin = User(username='admin', password=hashed_password, is_admin=True, is_approved=True)
            db.session.add(admin)
            db.session.commit()
    app.run(debug=True)